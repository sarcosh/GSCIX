# RabbitMQ Configuration for GSCIX/OpenCTI
# See https://www.rabbitmq.com/configure.html

# Consumer timeout: 1 hour (needed for long-running OpenCTI operations)
consumer_timeout = 3600000

# Management plugin: listen on all interfaces
management.tcp.port = 15672

# Default queue type
default_vhost = /
