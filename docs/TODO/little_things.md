




4- Cuando se carga el grafo del actor "Strategic Divergence Test Actor" no se muestran todas las pastillas de los elementos mostrados en el grafo. Parece que solo se están mostrando las pastillas de los elementos que están relacionados con el actor. Sería conveniente que se mostraran todas las pastillas de los elementos mostrados en el grafo.

5- No es necesario los "Analytical Filters" y por lo tanto se pueden eliminar. En su lugar estaría bien que hubiera un filtro para poder especificar dos fechas ("Desde" y "Hasta") y que se muestren los elementos que están dentro de ese rango de fechas.

6- En la vista "Influence Graph" cuando se elimina la sección derecha (detalle del actor, etc...) solo se puede recuperar seleccionando el actor. Sería conveniente que se pudiera recuperar. Además cuando se selecciona cualquier otro elemento del grafo, la sección derecha no se muestra; y se debería mostrar con la selección de la pastilla del elemento.

7- Hay elementos que no se muestran en el grafo. Por ejemplo, los elementos de tipo "x-influence-vector" no se muestran. Tampoco se muestran los elementos de tipo "intrusion-set" ni las relaciones con los mismos. Crea un icono específico para estos elementos.

8- En el caso de las pastillas de los Intrusion Sets, cuando se selecciona y se despliega el detalle, se debería mostrar la información relevante obtenida desde OpenCTI y además debería haber un botón de "View in OpenCTI" que permita visualizar el Intrusion Set en OpenCTI. 

9- Los iconos han de ser un poco más pequeños y ser proporcionales al radio de las circunferencias de los nodos. El tamaño de las circunferencias no son las mismas y varia en función del tipo. Todas deberían tener el rádio correspondiente a la circunferencia de "Strategic Objectives"

10- En la vista "Geo-Strategic Actor Explorer" se debería modificar la implementación para que cuando haya una alerta, en lugar de representarla entre "Hybrid Pressure Index (HPI) Analysis" y "HPI Temporal Evolution" se represente en la posición del componente "Stable Baseline". Es decir, si no hay ningún tipo de alerta que mostrar se muestra el mensaje de "Stable Baseline" pero si existe algún tipo de alerta se muestra en ese lugar.

11- En la vista "Geo-Strategic Actor Explorer" en el detalle inferior del actor en lugar de mostrar "Other Actor" cuando el alignment es "Other" se debería mostrar "NOT ALIGNED ACTOR"



