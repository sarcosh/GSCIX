
Act as a Senior Cyber Intelligence Analyst. Before proceeding, evaluate whether you have the complete and up-to-date technical specification of the GSCIX (Global Security Cyber Intelligence Exchange) standard.

If you do NOT have total certainty regarding the GSCIX Custom STIX Schema (including mandatory fields, data types, and relationship structures), stop immediately and request the necessary documentation or a reference JSON schema from me.

If you confirm that you possess full knowledge of the standard, generate a JSON output strictly following the GSCIX Custom STIX Schema based on the provided information, adhering to these requirements:

- Schema Compliance: Ensure that all required fields, structures, and formats are correctly implemented without any deviation from the GSCIX standard.

- Temporal Accuracy: Populate the 'first_seen' and 'last_seen' fields based solely on verifiable evidence from the provided sources. Do not infer or hallucinate dates without explicit justification.

- Relational Integrity: Identify and define all relationships between entities in strict accordance with GSCIX taxonomy, preserving semantic accuracy, structural consistency, and traceability to the sources.
Analytical Rigor: Prioritize accuracy and source validation. The final output must meet senior-level intelligence standards, ensuring the data is structured for high-fidelity exchange.

